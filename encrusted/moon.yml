$schema: https://moonrepo.dev/schemas/moon.json

project:
  name: encrusted
  description: Rust game engine library

tasks:
  build:
    command: cargo build --release
    inputs:
      - src/**/*
      - Cargo.toml

  test:
    command: cargo test
