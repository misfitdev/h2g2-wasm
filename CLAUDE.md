# CLAUDE.md

Project conventions, workflow, and product/style guidance for this repo live
in [`AGENTS.md`](./AGENTS.md) — read it before starting work here. It covers
repo layout, the `just`/`mise` toolchain, `bd` task tracking, code style, and
the product's tone/visual constraints.

`bd prime` runs automatically at session start and pre-compact via hooks; its
output includes persistent project memories that take precedence over
anything stale in this file or `AGENTS.md`.
